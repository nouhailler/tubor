# Tubor v0.3 — Dépendances
# Architecture web : FastAPI backend + React frontend

# ── Backend Python (voir aussi backend/requirements.txt) ─────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0
pydantic>=2.0.0
yt-dlp>=2024.1.1

# ── Modules stdlib utilisés (aucune installation requise) ─────────────────────
# asyncio, contextlib, dataclasses, datetime, json, pathlib, re
# shutil, sqlite3, subprocess, sys, threading, time, traceback
# urllib.request, uuid

# ── Frontend Node.js (voir frontend/package.json) ─────────────────────────────
# react@^18.3.1, react-dom@^18.3.1
# vite@^5.4, typescript@^5.6, @vitejs/plugin-react@^4.3
